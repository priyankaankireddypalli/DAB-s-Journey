
resources:
  pipelines:
    pipeline_demopipeline:
      name: demopipeline
      configuration:
        spark.sql.ansi.enabled: "true"
      libraries:
        - glob:
            include: ../../src/pipelines/demopipeline/transformations/**
      schema: default
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog_name}
      serverless: true
      root_path: ../../src/pipelines/demopipeline

